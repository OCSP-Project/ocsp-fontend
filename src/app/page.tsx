export default function HomePage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-semibold">OCSP Frontend</h1>
      <p className="mt-2 text-gray-600">
        Project skeleton đã sẵn sàng. Điều hướng tới <code>/login</code> hoặc <code>/dashboard</code> để xem nhóm trang.
      </p>
    </main>
  );
}
